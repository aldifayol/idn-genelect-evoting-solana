/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "gill";

export const BALLOT_DISCRIMINATOR = new Uint8Array([
  3, 232, 121, 204, 232, 137, 138, 164,
]);

export function getBallotDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(BALLOT_DISCRIMINATOR);
}

export type Ballot = {
  discriminator: ReadonlyUint8Array;
  election: Address;
  candidate: Address;
  /** Encrypted vote data for additional privacy layer */
  encryptedVoteData: ReadonlyUint8Array;
  timestamp: bigint;
  /** Sequential ballot number for counting verification */
  ballotSequence: bigint;
  /** Receipt hash that voter can use to verify their vote was counted */
  verificationReceipt: string;
  bump: number;
};

export type BallotArgs = {
  election: Address;
  candidate: Address;
  /** Encrypted vote data for additional privacy layer */
  encryptedVoteData: ReadonlyUint8Array;
  timestamp: number | bigint;
  /** Sequential ballot number for counting verification */
  ballotSequence: number | bigint;
  /** Receipt hash that voter can use to verify their vote was counted */
  verificationReceipt: string;
  bump: number;
};

/** Gets the encoder for {@link BallotArgs} account data. */
export function getBallotEncoder(): Encoder<BallotArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["election", getAddressEncoder()],
      ["candidate", getAddressEncoder()],
      ["encryptedVoteData", fixEncoderSize(getBytesEncoder(), 32)],
      ["timestamp", getI64Encoder()],
      ["ballotSequence", getU64Encoder()],
      [
        "verificationReceipt",
        addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
      ],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: BALLOT_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link Ballot} account data. */
export function getBallotDecoder(): Decoder<Ballot> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["election", getAddressDecoder()],
    ["candidate", getAddressDecoder()],
    ["encryptedVoteData", fixDecoderSize(getBytesDecoder(), 32)],
    ["timestamp", getI64Decoder()],
    ["ballotSequence", getU64Decoder()],
    [
      "verificationReceipt",
      addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder()),
    ],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link Ballot} account data. */
export function getBallotCodec(): Codec<BallotArgs, Ballot> {
  return combineCodec(getBallotEncoder(), getBallotDecoder());
}

export function decodeBallot<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<Ballot, TAddress>;
export function decodeBallot<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<Ballot, TAddress>;
export function decodeBallot<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<Ballot, TAddress> | MaybeAccount<Ballot, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getBallotDecoder(),
  );
}

export async function fetchBallot<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<Ballot, TAddress>> {
  const maybeAccount = await fetchMaybeBallot(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeBallot<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<Ballot, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeBallot(maybeAccount);
}

export async function fetchAllBallot(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<Ballot>[]> {
  const maybeAccounts = await fetchAllMaybeBallot(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeBallot(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<Ballot>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeBallot(maybeAccount));
}
