/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type Option,
  type OptionOrNullable,
  type ReadonlyUint8Array,
} from "gill";

export const VOTER_CREDENTIAL_DISCRIMINATOR = new Uint8Array([
  173, 196, 155, 186, 118, 40, 56, 23,
]);

export function getVoterCredentialDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    VOTER_CREDENTIAL_DISCRIMINATOR,
  );
}

export type VoterCredential = {
  discriminator: ReadonlyUint8Array;
  election: Address;
  voterAuthority: Address;
  /** SHA-256 hash of voter NIK (National Identity Number) */
  voterNikHash: ReadonlyUint8Array;
  /** SHA-256 hash of combined biometric data (retina + face + fingerprint) */
  biometricHash: ReadonlyUint8Array;
  /** IPFS hash pointing to encrypted selfie photo + ID card */
  photoIpfsHash: string;
  isVerified: boolean;
  hasVoted: boolean;
  verificationTimestamp: bigint;
  voteTimestamp: Option<bigint>;
  /** AI confidence score (0-100) for audit purposes */
  aiConfidenceScore: number;
  /** Unique verification code for voter to confirm their registration */
  verificationCode: string;
  bump: number;
};

export type VoterCredentialArgs = {
  election: Address;
  voterAuthority: Address;
  /** SHA-256 hash of voter NIK (National Identity Number) */
  voterNikHash: ReadonlyUint8Array;
  /** SHA-256 hash of combined biometric data (retina + face + fingerprint) */
  biometricHash: ReadonlyUint8Array;
  /** IPFS hash pointing to encrypted selfie photo + ID card */
  photoIpfsHash: string;
  isVerified: boolean;
  hasVoted: boolean;
  verificationTimestamp: number | bigint;
  voteTimestamp: OptionOrNullable<number | bigint>;
  /** AI confidence score (0-100) for audit purposes */
  aiConfidenceScore: number;
  /** Unique verification code for voter to confirm their registration */
  verificationCode: string;
  bump: number;
};

/** Gets the encoder for {@link VoterCredentialArgs} account data. */
export function getVoterCredentialEncoder(): Encoder<VoterCredentialArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["election", getAddressEncoder()],
      ["voterAuthority", getAddressEncoder()],
      ["voterNikHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["biometricHash", fixEncoderSize(getBytesEncoder(), 32)],
      [
        "photoIpfsHash",
        addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
      ],
      ["isVerified", getBooleanEncoder()],
      ["hasVoted", getBooleanEncoder()],
      ["verificationTimestamp", getI64Encoder()],
      ["voteTimestamp", getOptionEncoder(getI64Encoder())],
      ["aiConfidenceScore", getU8Encoder()],
      [
        "verificationCode",
        addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder()),
      ],
      ["bump", getU8Encoder()],
    ]),
    (value) => ({ ...value, discriminator: VOTER_CREDENTIAL_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link VoterCredential} account data. */
export function getVoterCredentialDecoder(): Decoder<VoterCredential> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["election", getAddressDecoder()],
    ["voterAuthority", getAddressDecoder()],
    ["voterNikHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["biometricHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["photoIpfsHash", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["isVerified", getBooleanDecoder()],
    ["hasVoted", getBooleanDecoder()],
    ["verificationTimestamp", getI64Decoder()],
    ["voteTimestamp", getOptionDecoder(getI64Decoder())],
    ["aiConfidenceScore", getU8Decoder()],
    [
      "verificationCode",
      addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder()),
    ],
    ["bump", getU8Decoder()],
  ]);
}

/** Gets the codec for {@link VoterCredential} account data. */
export function getVoterCredentialCodec(): Codec<
  VoterCredentialArgs,
  VoterCredential
> {
  return combineCodec(getVoterCredentialEncoder(), getVoterCredentialDecoder());
}

export function decodeVoterCredential<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<VoterCredential, TAddress>;
export function decodeVoterCredential<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<VoterCredential, TAddress>;
export function decodeVoterCredential<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
):
  | Account<VoterCredential, TAddress>
  | MaybeAccount<VoterCredential, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getVoterCredentialDecoder(),
  );
}

export async function fetchVoterCredential<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<VoterCredential, TAddress>> {
  const maybeAccount = await fetchMaybeVoterCredential(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeVoterCredential<
  TAddress extends string = string,
>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<VoterCredential, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeVoterCredential(maybeAccount);
}

export async function fetchAllVoterCredential(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<VoterCredential>[]> {
  const maybeAccounts = await fetchAllMaybeVoterCredential(
    rpc,
    addresses,
    config,
  );
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeVoterCredential(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<VoterCredential>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) =>
    decodeVoterCredential(maybeAccount),
  );
}
